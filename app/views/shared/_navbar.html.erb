<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <!-- Brand -->
    <a class="navbar-brand" href="/">My Movie App</a>

    <!-- Toggler for Mobile View -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar Links -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link active" href="/movies">Movies</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/lists">Lists</a>
        </li>
      </ul>
      <!-- Search Bar -->
      <%= form_with url: search_movies_path, method: :get, local: true, class: "d-flex" do %>
        <%= text_field_tag :query, params[:query], placeholder: "Search Movies", class: "form-control me-2" %>
        <%= submit_tag "Search", class: "btn btn-outline-success" %>
      <% end %>
    </div>
  </div>
</nav>
